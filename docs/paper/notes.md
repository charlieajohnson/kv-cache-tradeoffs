# Paper notes

- Maintain strict terminology: token length = L, attention heads = H, attention head size = d_k.
- Report memory growth as both bytes and MB.
- Use three seeds minimum for every key result.
- Archive raw run metadata with command, git hash, env hash, and config path.
