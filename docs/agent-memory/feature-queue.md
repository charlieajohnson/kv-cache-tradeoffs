# Feature queue

## In progress

- Finish benchmark loop hardening for cache reuse and decode timing edges.
- Add artifact metadata/version capture to non-plot CLI paths.

## Planned

- Add structured run manifest JSON schema and ingestion checks.
- Add deterministic synthetic dataset generators for edge-stress tasks.
- Add artifact promotion check in CI.
- Move quality suite to real checkpoints or explicit synthetic disclosure in docs.

## Done

- Create repo-level project scaffold.
- Add internal memory/process documentation.
- Add base CLI and module stubs for benchmark orchestration.
- Implement incremental KV cache in MHA/MQA/GQA layers.
- Wire synthetic KV-scaling, throughput, and latency benchmark execution.
