# Feature queue

## In progress

- Scaffold full codebase and experiment modules.
- Add experiment queue defaults under `configs/bench`.

## Planned

- Add structured run manifest JSON schema and ingestion checks.
- Add deterministic synthetic dataset generators for edge-stress tasks.
- Add artifact promotion check in CI.

## Done

- Create repo-level project scaffold.
- Add internal memory/process documentation.
- Add base CLI and module stubs for benchmark orchestration.
